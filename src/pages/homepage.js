import HSButton from "../components/button";

const HomePage = () => {
  return (
    <>
      <HSButton variant={"primary"} size={"small"} shape={"shape"}>
        로그인
      </HSButton>
    </>
  );
};

export default HomePage;
